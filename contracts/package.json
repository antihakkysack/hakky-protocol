{
  "name": "@hakky/contracts",
  "version": "0.1.0",
  "description": "Hakky Protocol smart contracts — 1:1 BTC-backed Clean BTC (cBTC), proof-of-reserves, and on-chain cleanliness attestations.",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.js",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "hardhat": "^2.29.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
